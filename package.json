{
  "name": "task-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace=mobile",
    "web": "npm run dev --workspace=web",
    "shared": "npm run build --workspace=shared"
  },
  "devDependencies": {
    "eas-cli": "^16.28.0",
    "typescript": "^5.3.3"
  }
}
